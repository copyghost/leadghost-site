// Shared site config.
// BOOKING_URL is the OneCal embed source; BOOKING_ANCHOR is where the CTAs
// smooth-scroll to (the inline booking section). Both the page and the
// conversion tracker import from here so nothing drifts.
export const BOOKING_URL = 'https://app.onecal.io/b/leadghost/30-minute-meeting';
export const BOOKING_ANCHOR = '#book';
export const CTA = 'Book a call';
