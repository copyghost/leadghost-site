// Shared site config. The booking link lives here so both the page CTAs and
// the conversion tracker reference the same value.
export const BOOKING_URL = 'https://app.onecal.io/b/leadghost/30-minute-meeting';
export const CTA = 'Book a call';
